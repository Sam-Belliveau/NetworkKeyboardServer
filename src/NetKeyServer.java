import java.awt.Frame;
import edu.stuylib.input.keyboard.server.NetKeyWindow;

public class NetKeyServer {
    public static void main(String[] args) {
        Frame window = new NetKeyWindow();
    }
}
